# @machinery/processors

This package contains the processor descriptions for different architectures in our standard description.

These descriptions can be used to generate virtual machines, assemblers, disassemblers, and emulators for these targets.
