# Assemblers

This package includes an assembly generator that can take a particular Syntax and
a particular architecture Target and generate a parser for an assembler to parse
assembly code. It then can use this in conjunction with the runtime Encoder to
generate machine code from assembly sources.
