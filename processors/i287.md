# i287

This is a floating-point implementation that has 80-bit floating point values governed by IEEE-754.

One can define a Floating data that is 80-bits wide and this will trigger the floating-point emulation for performing math on that value.
